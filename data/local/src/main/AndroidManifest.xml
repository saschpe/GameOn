<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:name=".DataContentProvider"
            android:authorities="${applicationId}.data_content_provider"
            android:exported="false" />

    </application>

</manifest>
